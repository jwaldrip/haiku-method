{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "https://haikumethod.ai/schemas/providers/canva.schema.json",
	"title": "Canva Provider Config",
	"description": "Provider-specific configuration for Canva design provider",
	"type": "object",
	"properties": {
		"brand_kit_id": {
			"type": "string",
			"description": "Canva Brand Kit ID for consistent branding"
		},
		"default_folder": {
			"type": "string",
			"description": "Default Canva folder path for organizing designs"
		},
		"export_format": {
			"type": "string",
			"enum": ["png", "jpg", "pdf"],
			"default": "png",
			"description": "Default export format for design assets"
		},
		"url_template": {
			"type": "string",
			"description": "URL template for linking to Canva designs. Variables: {key} (Canva design ID)",
			"default": "https://canva.com/design/{key}"
		}
	},
	"additionalProperties": false
}
