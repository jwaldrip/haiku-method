{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "https://haiku.dev/schemas/providers/slack.schema.json",
	"title": "Slack Provider Config",
	"description": "Provider-specific configuration for Slack comms provider",
	"type": "object",
	"properties": {
		"channel": {
			"type": "string",
			"description": "Default Slack channel for notifications"
		},
		"workspace_id": {
			"type": "string",
			"description": "Slack workspace ID"
		},
		"thread_replies": {
			"type": "boolean",
			"default": true,
			"description": "Whether to post status updates as thread replies instead of new messages"
		}
	},
	"additionalProperties": false
}
