{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "https://haikumethod.ai/schemas/providers/penpot.schema.json",
	"title": "Penpot Provider Config",
	"description": "Provider-specific configuration for Penpot design provider",
	"type": "object",
	"properties": {
		"instance_url": {
			"type": "string",
			"format": "uri",
			"description": "URL of the Penpot instance"
		},
		"project_id": {
			"type": "string",
			"description": "Penpot project ID"
		},
		"mcp_port": {
			"type": "integer",
			"default": 4401,
			"description": "Port number for the Penpot MCP server"
		},
		"url_template": {
			"type": "string",
			"description": "URL template for linking to Penpot designs. Variables: {key} (Penpot design ID or view token)",
			"default": "https://design.penpot.app/#/view/{key}"
		}
	},
	"additionalProperties": false
}
