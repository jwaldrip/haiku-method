# yaml-language-server: $schema=../schemas/settings.schema.json
#
# AI-DLC Settings Example
# Place this file at: .ai-dlc/settings.yml
#
# These settings configure how AI-DLC manages version control for your project.
# Settings can be overridden per-intent by adding git: or jj: keys to intent.md frontmatter.

# Git configuration (used when repository is git-based)
git:
  # How changes are organized into branches:
  # - trunk: All work on main branch, no feature branches
  # - unit: One branch per unit of work (default, recommended for most teams)
  # - intent: One long-lived branch per intent with linear history
  change_strategy: unit

  # Whether to create a PR for reviewing the elaborated intent before planning.
  # Useful for getting stakeholder buy-in on scope before implementation.
  elaboration_review: true

  # Default branch name. Set to 'auto' to detect from origin/HEAD,
  # or specify explicitly (e.g., 'main', 'master', 'develop')
  default_branch: auto

  # Uncomment to automatically merge completed unit branches to default branch
  # auto_merge: true

  # Uncomment to squash commits when auto-merging (only applies when auto_merge is true)
  # auto_squash: true

# Jujutsu (jj) configuration (used when repository uses jj)
# jj:
#   change_strategy: unit
#   elaboration_review: true
#   default_branch: auto

# Provider Configuration (optional)
# Configure external system integrations for ticket sync, spec references, and more.
# providers:
#   ticketing:
#     type: jira                # jira | linear | github-issues | gitlab-issues
#     config:
#       project_key: "PROJ"
#     instructions: |
#       - Use Fibonacci story points (1, 2, 3, 5, 8, 13)
#       - Always set component to "Backend" or "Frontend"
#
#   design:
#     type: figma               # figma
#     instructions: |
#       - Only reference designs marked "Ready for Dev"
#
#   spec:
#     type: notion              # notion | confluence | google-docs
#     config:
#       workspace_id: "your-workspace-id"
#
#   comms:
#     type: slack               # slack | teams | discord
#     config:
#       channel: "#dev-updates"

# Infrastructure Stack Configuration (optional)
# Describes the deployment and operations layers of your project.
# All layers are optional. An empty stack ({}) is valid.
# stack:
#   infrastructure:
#     - provider: terraform       # terraform | cloudformation | pulumi | none
#       scope: infra/
#       state_backend: s3
#   compute:
#     - provider: kubernetes      # kubernetes | ecs | lambda | docker-compose | none
#       managed_by: eks
#       registry: 123456789.dkr.ecr.us-east-1.amazonaws.com
#   packaging:
#     - provider: helm            # helm | kustomize | raw-manifests | none
#       charts_dir: charts/
#   pipeline:
#     - provider: github-actions  # github-actions | gitlab-ci | jenkins | circleci | none
#       workflows_dir: .github/workflows/
#   secrets:
#     - provider: vault           # vault | aws-sm | gcp-sm | env | none
#   monitoring:
#     - provider: datadog         # prometheus | datadog | cloudwatch | newrelic | otel | none
#   alerting:
#     - provider: pagerduty       # pagerduty | opsgenie | datadog | prometheus-alertmanager | none
#       routing: backend-team
#   operations:
#     runtime: node               # node | python | go | shell (auto-detected if omitted)
#     scheduled:
#       - name: cleanup
#         schedule: "0 3 * * *"
#         command: node scripts/cleanup.js
#     reactive:
#       - name: alert-handler
#         trigger: webhook
#         command: node scripts/handle-alert.js

# Quality Gates (backpressure checks)
# These commands run during execution to ensure code quality.
# quality_gates:
#   test:
#     command: npm test
#   lint:
#     command: npx eslint .
#   typecheck:
#     command: npx tsc --noEmit
#   build:
#     command: npm run build
#   deploy_validate:
#     command: ./scripts/validate-deploy.sh
#   container_build:
#     command: docker build .
#   manifest_validate:
#     command: helm template charts/ | kubeval
#   observable_validate:
#     command: ./scripts/validate-observability.sh
#   operations_validate:
#     command: ./scripts/validate-operations.sh --dry-run

# Review Agents
# Configure which specialized review agents run for this project.
# review_agents:
#   security: true                     # default: true
#   performance: true                  # default: true
#   architecture: true                 # default: true
#   correctness: true                  # default: true
#   test_quality: true                 # default: true
#   data_integrity: false              # default: false
#   schema_drift: false                # default: false
#   accessibility: false               # default: false
#   api_contract: false                # default: false
#   design_system: false               # default: false
#   deployment_safety: false           # default: false
#   observability_completeness: false  # default: false
#   infrastructure_correctness: false  # default: false
