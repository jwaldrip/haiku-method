{
  "name": "@haiku/haiku",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --format=esm --outfile=../../plugin/bin/haiku --banner:js=\"#!/usr/bin/env node\nimport{createRequire}from'module';const require=createRequire(import.meta.url);\" && chmod +x ../../plugin/bin/haiku",
    "typecheck": "tsc --noEmit",
    "test": "node test/state-tools.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.5",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.7.0"
  }
}
